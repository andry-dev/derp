defmodule Derp.Repo.Migrations.CreateStores do
  use Ecto.Migration

  def change do
    create table(:stores) do
      add :name, :string
      add :id, :integer

      timestamps()
    end
  end
end
